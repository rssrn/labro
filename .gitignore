# Python build artefacts
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Virtual environment
.venv/

# Coverage
.coverage
htmlcov/

# mypy cache
.mypy_cache/

# ruff cache
.ruff_cache/

# pytest cache
.pytest_cache/

# pip-audit marker
.pip-audit-last-run

# Editor
.idea/
.vscode/
*.swp
*~

# OS
.DS_Store

# Claude Code session files (dropped by agent during live runs)
.antigravitycli/
.claude/

# Playwright MCP screenshots/snapshots (local dev only)
.playwright-mcp/

# User-specific config (lives in a separate private repo — see labro.example.toml)
labro.toml

# Local SQLite database (runtime artifact; production path is /data/labro.db in container)
*.db
